<ufp:BasePage x:Class="Heimatplatz.Features.Properties.Presentation.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Heimatplatz.Features.Properties.Presentation"
      xmlns:controls="using:Heimatplatz.Features.Properties.Controls"
      xmlns:models="using:Heimatplatz.Features.Properties.Contracts.Models"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:uc="using:UnoFramework.Controls"
      xmlns:ufp="using:UnoFramework.Pages"
      NavigationCacheMode="Required">

    <!--
    HomePage - Premium Editorial Design
    Elegantes Layout mit großzügigem Weißraum
    Header wurde in Shell verschoben (AppHeader Control)
    -->

    <Grid x:Name="RootGrid"
          Background="{ThemeResource BackgroundBrush}"
          AutomationProperties.AutomationId="HomePage_Root">

        <uc:BusyOverlay IsBusy="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                        BusyMessage="{x:Bind ViewModel.BusyMessage, Mode=OneWay}"
                        AutomationProperties.AutomationId="HomePage_BusyOverlay">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Desktop Filter ist jetzt im AppHeader (HeaderCenter Region) -->

                <!-- Mobile Filter (unter 905px) — einklappbar -->
                <Border Grid.Row="0"
                        Background="{ThemeResource SurfaceBrush}"
                        BorderBrush="{ThemeResource BorderBrush}"
                        BorderThickness="0,0,0,1"
                        Visibility="{utu:Responsive Narrowest=Visible, Narrow=Visible, Normal=Visible, Wide=Visible, Widest=Collapsed}"
                        AutomationProperties.AutomationId="HomePage_MobileFilter">
                    <StackPanel>
                        <!-- Filter Header: Immer sichtbar — ganzer Bereich klickbar -->
                        <Button Command="{x:Bind ViewModel.ToggleFilterExpandedCommand}"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Stretch"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="12,8"
                                MinHeight="44"
                                AutomationProperties.AutomationId="HomePage_MobileFilterToggle">
                            <Grid AutomationProperties.AutomationId="HomePage_MobileFilterHeader">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Filter-Icon + Label -->
                                <StackPanel Grid.Column="0"
                                            Orientation="Horizontal"
                                            Spacing="6"
                                            VerticalAlignment="Center">
                                    <FontIcon Glyph="&#xE71C;"
                                              FontSize="14"
                                              Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                                    <TextBlock Text="Filter"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="{ThemeResource OnSurfaceBrush}"
                                               VerticalAlignment="Center" />
                                </StackPanel>

                                <!-- Ergebnis-Counter -->
                                <TextBlock Grid.Column="1"
                                           Text="{x:Bind ViewModel.ResultCountText, Mode=OneWay}"
                                           FontWeight="SemiBold"
                                           Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                           FontSize="12"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center"
                                           Margin="0,0,8,0"
                                           AutomationProperties.AutomationId="HomePage_MobileResultCount" />

                                <!-- Chevron-Icon: Auf/Zuklappen -->
                                <FontIcon Grid.Column="2"
                                          Glyph="{x:Bind ViewModel.FilterToggleGlyph, Mode=OneWay}"
                                          FontSize="12"
                                          Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                          VerticalAlignment="Center" />
                            </Grid>
                        </Button>

                        <!-- Filter Body: Einklappbar -->
                        <StackPanel Spacing="8"
                                    Padding="12,0,12,8"
                                    Visibility="{x:Bind ViewModel.IsFilterExpanded, Mode=OneWay}"
                                    AutomationProperties.AutomationId="HomePage_MobileFilterBody">

                            <!-- OrtPicker und Alters-Filter -->
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <controls:OrtPicker Bezirke="{x:Bind ViewModel.Bezirke, Mode=OneWay}"
                                                    SelectedOrte="{x:Bind ViewModel.SelectedOrte, Mode=TwoWay}"
                                                    HorizontalAlignment="Stretch"
                                                    AutomationProperties.AutomationId="HomePage_MobileOrtPicker" />
                                <controls:AgeFilterPicker SelectedAgeFilter="{x:Bind ViewModel.SelectedAgeFilter, Mode=TwoWay}"
                                                          HorizontalAlignment="Stretch"
                                                          AutomationProperties.AutomationId="HomePage_MobileAgeFilterPicker" />
                            </StackPanel>

                            <!-- Filter Chips: Immobilientyp -->
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <controls:FilterChip Label="Haus"
                                                     IsSelected="{x:Bind ViewModel.IsHausSelected, Mode=TwoWay}" />
                                <controls:FilterChip Label="Grund"
                                                     IsSelected="{x:Bind ViewModel.IsGrundstueckSelected, Mode=TwoWay}" />
                                <controls:FilterChip Label="ZV"
                                                     IsSelected="{x:Bind ViewModel.IsZwangsversteigerungSelected, Mode=TwoWay}" />
                            </StackPanel>

                            <!-- Anbietertyp Picker -->
                            <controls:SellerTypePicker SellerTypes="{x:Bind ViewModel.SellerTypes, Mode=OneWay}"
                                                       AutomationProperties.AutomationId="HomePage_MobileSellerTypePicker" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Toolbar: Ergebnis-Count + Sortierung (immer sichtbar) -->
                <Border Grid.Row="1"
                        Background="{ThemeResource SurfaceBrush}"
                        Padding="{utu:Responsive Narrowest='12,8', Normal='20,8'}"
                        BorderBrush="{ThemeResource BorderBrush}"
                        BorderThickness="0,0,0,0.5"
                        AutomationProperties.AutomationId="HomePage_Toolbar">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Ergebnis-Counter (Desktop) -->
                        <TextBlock Text="{x:Bind ViewModel.ResultCountText, Mode=OneWay}"
                                   FontSize="13"
                                   Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                   VerticalAlignment="Center" />

                        <!-- Sort Button -->
                        <Button Grid.Column="1"
                                Background="{ThemeResource SolidBackgroundFillColorSecondary}"
                                BorderBrush="{ThemeResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource RadiusSm}"
                                Padding="12,6"
                                MinHeight="36"
                                AutomationProperties.AutomationId="HomePage_SortButton">
                            <Button.Flyout>
                                <MenuFlyout Placement="BottomEdgeAlignedRight">
                                    <MenuFlyoutItem Text="Neueste" Command="{x:Bind ViewModel.SetSortCommand}" CommandParameter="Neueste" />
                                    <MenuFlyoutItem Text="Älteste" Command="{x:Bind ViewModel.SetSortCommand}" CommandParameter="Aelteste" />
                                    <MenuFlyoutItem Text="Preis ↑" Command="{x:Bind ViewModel.SetSortCommand}" CommandParameter="PreisAuf" />
                                    <MenuFlyoutItem Text="Preis ↓" Command="{x:Bind ViewModel.SetSortCommand}" CommandParameter="PreisAb" />
                                    <MenuFlyoutItem Text="Fläche ↓" Command="{x:Bind ViewModel.SetSortCommand}" CommandParameter="FlaecheAb" />
                                    <MenuFlyoutItem Text="Fläche ↑" Command="{x:Bind ViewModel.SetSortCommand}" CommandParameter="FlaecheAuf" />
                                    <MenuFlyoutItem Text="PLZ" Command="{x:Bind ViewModel.SetSortCommand}" CommandParameter="PlzAuf" />
                                </MenuFlyout>
                            </Button.Flyout>
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE8CB;"
                                          FontSize="12"
                                          Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                                <TextBlock Text="{x:Bind ViewModel.SortLabel, Mode=OneWay}"
                                           FontSize="13"
                                           FontWeight="Medium"
                                           Foreground="{ThemeResource OnSurfaceBrush}" />
                            </StackPanel>
                        </Button>
                    </Grid>
                </Border>

                <!-- Content: Max 20 items per page, no incremental loading -->
                <ScrollViewer x:Name="ContentScrollViewer"
                              Grid.Row="2"
                              Padding="{utu:Responsive Narrowest='12', Normal='20,16'}"
                              AutomationProperties.AutomationId="HomePage_ScrollViewer">
                    <ItemsRepeater ItemsSource="{x:Bind ViewModel.Properties, Mode=OneWay}"
                                   AutomationProperties.AutomationId="HomePage_PropertyList">
                        <ItemsRepeater.Layout>
                            <UniformGridLayout Orientation="Horizontal"
                                               MinItemWidth="240"
                                               MinItemHeight="330"
                                               MinRowSpacing="16"
                                               MinColumnSpacing="16"
                                               ItemsStretch="Fill" />
                        </ItemsRepeater.Layout>
                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate x:DataType="models:PropertyListItemDto">
                                <controls:PropertyCard Property="{x:Bind}"
                                                       IsAuthenticated="{Binding DataContext.IsAuthenticated, ElementName=RootGrid}"
                                                       CardClicked="OnPropertyCardClicked"
                                                       PropertyBlocked="OnPropertyBlocked"
                                                       PropertyFavorited="OnPropertyFavorited"
                                                       AutomationProperties.AutomationId="HomePage_PropertyCard" />
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                </ScrollViewer>

                <!-- Empty State: Schlicht -->
                <StackPanel Grid.Row="2"
                            Visibility="{x:Bind ViewModel.EmptyVisibility, Mode=OneWay}"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Spacing="8"
                            AutomationProperties.AutomationId="HomePage_EmptyState">
                    <TextBlock Text="—"
                               FontSize="32"
                               Foreground="{ThemeResource OnSurfaceVariantBrush}"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="Keine Treffer"
                               FontSize="14"
                               Foreground="{ThemeResource OnSurfaceVariantBrush}"
                               HorizontalAlignment="Center"
                               AutomationProperties.AutomationId="HomePage_EmptyText" />
                </StackPanel>

                <!-- Pagination -->
                <Border Grid.Row="3"
                        Padding="8,6"
                        Visibility="{x:Bind ViewModel.PaginationVisibility, Mode=OneWay}"
                        AutomationProperties.AutomationId="HomePage_Pagination">
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                Spacing="16">
                        <Button Command="{x:Bind ViewModel.GoToPreviousPageCommand}"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="10,8"
                                MinWidth="0"
                                AutomationProperties.AutomationId="HomePage_PrevPage">
                            <FontIcon Glyph="&#xE76B;"
                                      FontSize="14"
                                      Foreground="{ThemeResource OnSurfaceBrush}" />
                        </Button>
                        <TextBlock Text="{x:Bind ViewModel.PageInfoText, Mode=OneWay}"
                                   VerticalAlignment="Center"
                                   FontSize="13"
                                   Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                        <Button Command="{x:Bind ViewModel.GoToNextPageCommand}"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="10,8"
                                MinWidth="0"
                                AutomationProperties.AutomationId="HomePage_NextPage">
                            <FontIcon Glyph="&#xE76C;"
                                      FontSize="14"
                                      Foreground="{ThemeResource OnSurfaceBrush}" />
                        </Button>
                    </StackPanel>
                </Border>

                <!-- Footer -->
                <Border Grid.Row="4"
                        utu:SafeArea.Insets="Bottom"
                        Padding="0,8,0,8"
                        BorderBrush="{ThemeResource BorderBrush}"
                        BorderThickness="0,1,0,0"
                        AutomationProperties.AutomationId="HomePage_Footer">
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                Spacing="24">
                        <HyperlinkButton Content="Impressum"
                                         uen:Navigation.Request="impressum"
                                         Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                         FontSize="12"
                                         AutomationProperties.AutomationId="HomePage_ImprintLink" />
                        <HyperlinkButton Content="Datenschutz"
                                         uen:Navigation.Request="datenschutz"
                                         Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                         FontSize="12"
                                         AutomationProperties.AutomationId="HomePage_PrivacyPolicyLink" />
                        <TextBlock Text="Heimatplatz"
                                   Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                   FontSize="12"
                                   Opacity="0.6"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Border>
            </Grid>
        </uc:BusyOverlay>
    </Grid>
</ufp:BasePage>
