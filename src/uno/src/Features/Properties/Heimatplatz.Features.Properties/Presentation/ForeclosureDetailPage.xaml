<pages:BasePage x:Class="Heimatplatz.Features.Properties.Presentation.ForeclosureDetailPage"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:local="using:Heimatplatz.Features.Properties.Presentation"
                xmlns:models="using:Heimatplatz.Features.Properties.Contracts.Models"
                xmlns:utu="using:Uno.Toolkit.UI"
                xmlns:uc="using:UnoFramework.Controls"
                xmlns:pages="using:UnoFramework.Pages"
                x:Name="RootPage"
                NavigationCacheMode="Disabled">

    <!--
    ForeclosureDetailPage - Zwangsversteigerung Detailansicht (Immoscout Style)
    Identisches Design-Pattern wie PropertyDetailPage
    -->

    <Grid Background="{ThemeResource BackgroundBrush}">
        <uc:BusyOverlay IsBusy="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                        BusyMessage="{x:Bind ViewModel.BusyMessage, Mode=OneWay}">

            <Grid>
                <!-- Content -->
                <ScrollViewer>
                    <Grid Padding="{utu:Responsive Narrowest='0', Normal='40,24'}"
                          MaxWidth="1200"
                          HorizontalAlignment="{utu:Responsive Narrowest=Stretch, Normal=Center}">

                        <!-- Mobile Layout -->
                        <Grid Visibility="{utu:Responsive Narrowest=Visible, Normal=Collapsed}">
                            <StackPanel>
                                <!-- Hero Image -->
                                <Grid Height="320">
                                    <Border Background="{ThemeResource PlaceholderBrush}">
                                        <FontIcon Glyph="&#xE8A9;"
                                                  FontSize="48"
                                                  Foreground="{ThemeResource DetailsBrush}"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Opacity="0.5" />
                                    </Border>
                                    <FlipView x:Name="MobileImageFlipView"
                                              ItemsSource="{x:Bind ViewModel.Property.ImageUrls, Mode=OneWay}"
                                              SelectionChanged="OnImageSelectionChanged">
                                        <FlipView.ItemTemplate>
                                            <DataTemplate>
                                                <Image Source="{Binding}"
                                                       Stretch="UniformToFill" />
                                            </DataTemplate>
                                        </FlipView.ItemTemplate>
                                    </FlipView>

                                    <!-- Gradient Overlay -->
                                    <Border IsHitTestVisible="False">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="#50000000" Offset="0" />
                                                <GradientStop Color="#00000000" Offset="0.25" />
                                                <GradientStop Color="#00000000" Offset="0.7" />
                                                <GradientStop Color="#40000000" Offset="1" />
                                            </LinearGradientBrush>
                                        </Border.Background>
                                    </Border>

                                    <!-- Typ-Badge (oben links) -->
                                    <Border Background="{x:Bind ViewModel.TypeBadgeBrush, Mode=OneWay}"
                                            CornerRadius="{StaticResource RadiusMd}"
                                            Padding="12,6"
                                            Margin="12"
                                            VerticalAlignment="Top"
                                            HorizontalAlignment="Left">
                                        <TextBlock Text="{x:Bind ViewModel.TypeBadgeText, Mode=OneWay}"
                                                   FontSize="10"
                                                   FontWeight="SemiBold"
                                                   CharacterSpacing="80"
                                                   Foreground="White" />
                                    </Border>

                                    <!-- Bild Counter (links unten) -->
                                    <Border Background="#80000000"
                                            CornerRadius="{StaticResource RadiusSm}"
                                            Padding="10,6"
                                            Margin="12"
                                            VerticalAlignment="Bottom"
                                            HorizontalAlignment="Left">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon Glyph="&#xE8B9;"
                                                      FontSize="12"
                                                      Foreground="White" />
                                            <TextBlock FontSize="13" FontWeight="Medium" Foreground="White">
                                                <Run Text="{x:Bind ViewModel.CurrentImageIndex, Mode=OneWay}" />
                                                <Run Text="/" />
                                                <Run Text="{x:Bind ViewModel.Property.ImageUrls.Count, Mode=OneWay}" />
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>

                                    <!-- Merken/Teilen Buttons (rechts oben) - Circular Design -->
                                    <StackPanel Orientation="Horizontal"
                                                Spacing="8"
                                                Margin="12"
                                                VerticalAlignment="Top"
                                                HorizontalAlignment="Right">
                                        <Button Background="#70000000"
                                                BorderThickness="0"
                                                Padding="0"
                                                Width="40"
                                                Height="40"
                                                CornerRadius="{StaticResource RadiusFull}"
                                                Visibility="{x:Bind ViewModel.IsAuthenticated, Mode=OneWay}"
                                                Command="{x:Bind ViewModel.ToggleFavoriteCommand}">
                                            <FontIcon Glyph="{x:Bind ViewModel.FavoriteButtonIcon, Mode=OneWay}"
                                                      FontSize="18"
                                                      Foreground="White" />
                                        </Button>
                                        <Button Background="#70000000"
                                                BorderThickness="0"
                                                Padding="0"
                                                Width="40"
                                                Height="40"
                                                CornerRadius="{StaticResource RadiusFull}"
                                                Command="{x:Bind ViewModel.SharePropertyCommand}">
                                            <FontIcon Glyph="&#xE72D;"
                                                      FontSize="18"
                                                      Foreground="White" />
                                        </Button>
                                    </StackPanel>
                                </Grid>

                                <!-- Content Section -->
                                <StackPanel Padding="16,24,16,24" Spacing="20" Background="{ThemeResource SurfaceBrush}">

                                    <!-- Strukturierte Datentabelle -->
                                    <ItemsRepeater ItemsSource="{x:Bind ViewModel.DetailSections, Mode=OneWay}">
                                        <ItemsRepeater.ItemTemplate>
                                            <DataTemplate x:DataType="models:PropertyDetailSection">
                                                <StackPanel Margin="0,0,0,16">
                                                    <!-- Section Header -->
                                                    <TextBlock Text="{x:Bind Title}"
                                                               FontSize="12"
                                                               FontWeight="SemiBold"
                                                               CharacterSpacing="60"
                                                               Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                                               Margin="0,0,0,8" />

                                                    <!-- Section Items -->
                                                    <Border Background="{ThemeResource SolidBackgroundFillColorSecondary}"
                                                            CornerRadius="{StaticResource RadiusMd}"
                                                            Padding="0">
                                                        <ItemsRepeater ItemsSource="{x:Bind Items}">
                                                            <ItemsRepeater.ItemTemplate>
                                                                <DataTemplate x:DataType="models:PropertyDetailItem">
                                                                    <StackPanel>
                                                                        <Grid Padding="14,10"
                                                                              ColumnSpacing="12">
                                                                            <Grid.ColumnDefinitions>
                                                                                <ColumnDefinition Width="*" />
                                                                                <ColumnDefinition Width="Auto" />
                                                                            </Grid.ColumnDefinitions>
                                                                            <TextBlock Grid.Column="0"
                                                                                       Text="{x:Bind Label}"
                                                                                       FontSize="14"
                                                                                       FontWeight="SemiBold"
                                                                                       Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                                                                       VerticalAlignment="Center" />
                                                                            <TextBlock Grid.Column="1"
                                                                                       Text="{x:Bind Value}"
                                                                                       FontSize="14"
                                                                                       Foreground="{ThemeResource OnSurfaceBrush}"
                                                                                       VerticalAlignment="Center"
                                                                                       TextAlignment="Right" />
                                                                        </Grid>
                                                                        <!-- Trennlinie -->
                                                                        <Border Height="1"
                                                                                Background="{ThemeResource BorderBrush}"
                                                                                Opacity="0.3"
                                                                                Margin="14,0" />
                                                                    </StackPanel>
                                                                </DataTemplate>
                                                            </ItemsRepeater.ItemTemplate>
                                                        </ItemsRepeater>
                                                    </Border>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsRepeater.ItemTemplate>
                                    </ItemsRepeater>

                                    <!-- Beschreibung / Hinweise -->
                                    <StackPanel Spacing="8"
                                                Visibility="{x:Bind ViewModel.HasDescription, Mode=OneWay}">
                                        <TextBlock Text="BESCHREIBUNG / HINWEISE"
                                                   FontSize="12"
                                                   FontWeight="SemiBold"
                                                   CharacterSpacing="60"
                                                   Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                                        <TextBlock Text="{x:Bind ViewModel.Description, Mode=OneWay}"
                                                   FontSize="14"
                                                   Foreground="{ThemeResource OnSurfaceBrush}"
                                                   TextWrapping="Wrap"
                                                   LineHeight="22" />
                                    </StackPanel>

                                    <!-- Dokumente -->
                                    <StackPanel Spacing="10"
                                                Visibility="{x:Bind ViewModel.HasDocuments, Mode=OneWay}">
                                        <TextBlock Text="DOKUMENTE"
                                                   FontSize="12"
                                                   FontWeight="SemiBold"
                                                   CharacterSpacing="60"
                                                   Foreground="{ThemeResource OnSurfaceVariantBrush}" />

                                        <StackPanel Spacing="8">
                                            <Button HorizontalAlignment="Stretch"
                                                    HorizontalContentAlignment="Left"
                                                    Padding="14,12"
                                                    Background="{ThemeResource SolidBackgroundFillColorSecondary}"
                                                    CornerRadius="{StaticResource RadiusMd}"
                                                    BorderThickness="0"
                                                    CommandParameter="{x:Bind ViewModel.FloorPlanUrl, Mode=OneWay}"
                                                    Command="{x:Bind ViewModel.OpenDocumentCommand}"
                                                    Visibility="{x:Bind ViewModel.FloorPlanUrl, Converter={StaticResource NullToCollapsedConverter}, Mode=OneWay}">
                                                <StackPanel Orientation="Horizontal" Spacing="10">
                                                    <FontIcon Glyph="&#xE8A5;" FontSize="16" Foreground="{ThemeResource AccentBrush}" />
                                                    <TextBlock Text="Grundriss" FontSize="14" FontWeight="Medium" VerticalAlignment="Center" />
                                                </StackPanel>
                                            </Button>

                                            <Button HorizontalAlignment="Stretch"
                                                    HorizontalContentAlignment="Left"
                                                    Padding="14,12"
                                                    Background="{ThemeResource SolidBackgroundFillColorSecondary}"
                                                    CornerRadius="{StaticResource RadiusMd}"
                                                    BorderThickness="0"
                                                    CommandParameter="{x:Bind ViewModel.SitePlanUrl, Mode=OneWay}"
                                                    Command="{x:Bind ViewModel.OpenDocumentCommand}"
                                                    Visibility="{x:Bind ViewModel.SitePlanUrl, Converter={StaticResource NullToCollapsedConverter}, Mode=OneWay}">
                                                <StackPanel Orientation="Horizontal" Spacing="10">
                                                    <FontIcon Glyph="&#xE909;" FontSize="16" Foreground="{ThemeResource AccentBrush}" />
                                                    <TextBlock Text="Lageplan" FontSize="14" FontWeight="Medium" VerticalAlignment="Center" />
                                                </StackPanel>
                                            </Button>

                                            <Button HorizontalAlignment="Stretch"
                                                    HorizontalContentAlignment="Left"
                                                    Padding="14,12"
                                                    Background="{ThemeResource SolidBackgroundFillColorSecondary}"
                                                    CornerRadius="{StaticResource RadiusMd}"
                                                    BorderThickness="0"
                                                    CommandParameter="{x:Bind ViewModel.LongAppraisalUrl, Mode=OneWay}"
                                                    Command="{x:Bind ViewModel.OpenDocumentCommand}"
                                                    Visibility="{x:Bind ViewModel.LongAppraisalUrl, Converter={StaticResource NullToCollapsedConverter}, Mode=OneWay}">
                                                <StackPanel Orientation="Horizontal" Spacing="10">
                                                    <FontIcon Glyph="&#xE9F9;" FontSize="16" Foreground="{ThemeResource AccentBrush}" />
                                                    <TextBlock Text="Langschaetzung" FontSize="14" FontWeight="Medium" VerticalAlignment="Center" />
                                                </StackPanel>
                                            </Button>

                                            <Button HorizontalAlignment="Stretch"
                                                    HorizontalContentAlignment="Left"
                                                    Padding="14,12"
                                                    Background="{ThemeResource SolidBackgroundFillColorSecondary}"
                                                    CornerRadius="{StaticResource RadiusMd}"
                                                    BorderThickness="0"
                                                    CommandParameter="{x:Bind ViewModel.ShortAppraisalUrl, Mode=OneWay}"
                                                    Command="{x:Bind ViewModel.OpenDocumentCommand}"
                                                    Visibility="{x:Bind ViewModel.ShortAppraisalUrl, Converter={StaticResource NullToCollapsedConverter}, Mode=OneWay}">
                                                <StackPanel Orientation="Horizontal" Spacing="10">
                                                    <FontIcon Glyph="&#xE9F9;" FontSize="16" Foreground="{ThemeResource AccentBrush}" />
                                                    <TextBlock Text="Kurzschaetzung" FontSize="14" FontWeight="Medium" VerticalAlignment="Center" />
                                                </StackPanel>
                                            </Button>
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Kontaktbox -->
                                    <Border Background="{ThemeResource SurfaceBrush}"
                                            BorderBrush="{ThemeResource BorderBrush}"
                                            BorderThickness="0.5"
                                            CornerRadius="{StaticResource RadiusMd}"
                                            Padding="20"
                                            Margin="0,4,0,0"
                                            Translation="0,0,12">
                                        <Border.Shadow>
                                            <ThemeShadow />
                                        </Border.Shadow>
                                        <StackPanel Spacing="16">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0" Spacing="6">
                                                    <TextBlock Text="Gericht"
                                                               FontSize="12"
                                                               Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                                                    <TextBlock Text="{x:Bind ViewModel.Property.SellerName, Mode=OneWay}"
                                                               FontSize="16"
                                                               FontWeight="SemiBold"
                                                               Foreground="{ThemeResource OnSurfaceBrush}"
                                                               TextWrapping="Wrap" />
                                                </StackPanel>

                                                <!-- Court Icon -->
                                                <Border Grid.Column="1"
                                                        Width="56"
                                                        Height="56"
                                                        CornerRadius="{StaticResource RadiusFull}"
                                                        Background="{ThemeResource PlaceholderBrush}"
                                                        Margin="16,0,0,0">
                                                    <FontIcon Glyph="&#xE913;"
                                                              FontSize="24"
                                                              Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                                              HorizontalAlignment="Center"
                                                              VerticalAlignment="Center" />
                                                </Border>
                                            </Grid>

                                            <TextBlock Text="{x:Bind ViewModel.CopyFeedback, Mode=OneWay}"
                                                       FontSize="12"
                                                       FontWeight="SemiBold"
                                                       Foreground="{ThemeResource AccentBrush}"
                                                       HorizontalAlignment="Center"
                                                       Visibility="{x:Bind ViewModel.CopyFeedback, Converter={StaticResource NullToCollapsedConverter}, Mode=OneWay}" />

                                            <!-- Edikt Button -->
                                            <Button Background="{ThemeResource OnSurfaceBrush}"
                                                    Foreground="{ThemeResource SurfaceBrush}"
                                                    FontWeight="SemiBold"
                                                    FontSize="15"
                                                    Height="48"
                                                    HorizontalAlignment="Stretch"
                                                    HorizontalContentAlignment="Center"
                                                    CornerRadius="{StaticResource RadiusMd}"
                                                    Command="{x:Bind ViewModel.OpenEdictCommand}"
                                                    Visibility="{x:Bind ViewModel.EdictUrl, Converter={StaticResource NullToCollapsedConverter}, Mode=OneWay}">
                                                <StackPanel Orientation="Horizontal" Spacing="10">
                                                    <FontIcon Glyph="&#xE8A7;"
                                                              FontSize="16"
                                                              Foreground="{ThemeResource SurfaceBrush}" />
                                                    <TextBlock Text="Edikt ansehen"
                                                               FontWeight="SemiBold"
                                                               VerticalAlignment="Center" />
                                                </StackPanel>
                                            </Button>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Grid>

                        <!-- Desktop Layout -->
                        <StackPanel Visibility="{utu:Responsive Narrowest=Collapsed, Normal=Visible}"
                                    Spacing="24">

                            <!-- Hero Image (volle Breite) mit Schatten -->
                            <Border CornerRadius="{StaticResource RadiusMd}" Height="480"
                                    Translation="0,0,16">
                                <Border.Shadow>
                                    <ThemeShadow />
                                </Border.Shadow>
                                <Border.Clip>
                                    <RectangleGeometry Rect="0,0,10000,480" />
                                </Border.Clip>
                                <Grid>
                                    <Border Background="{ThemeResource PlaceholderBrush}"
                                            CornerRadius="{StaticResource RadiusMd}">
                                        <FontIcon Glyph="&#xE8A9;"
                                                  FontSize="64"
                                                  Foreground="{ThemeResource DetailsBrush}"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Opacity="0.5" />
                                    </Border>
                                    <FlipView x:Name="DesktopImageFlipView"
                                              ItemsSource="{x:Bind ViewModel.Property.ImageUrls, Mode=OneWay}"
                                              SelectionChanged="OnImageSelectionChanged">
                                        <FlipView.ItemTemplate>
                                            <DataTemplate>
                                                <Image Source="{Binding}"
                                                       Stretch="UniformToFill" />
                                            </DataTemplate>
                                        </FlipView.ItemTemplate>
                                    </FlipView>

                                    <!-- Gradient Overlay -->
                                    <Border CornerRadius="{StaticResource RadiusMd}" IsHitTestVisible="False">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="#40000000" Offset="0" />
                                                <GradientStop Color="#00000000" Offset="0.2" />
                                                <GradientStop Color="#00000000" Offset="0.75" />
                                                <GradientStop Color="#30000000" Offset="1" />
                                            </LinearGradientBrush>
                                        </Border.Background>
                                    </Border>

                                    <!-- Typ-Badge (oben links) -->
                                    <Border Background="{x:Bind ViewModel.TypeBadgeBrush, Mode=OneWay}"
                                            CornerRadius="{StaticResource RadiusMd}"
                                            Padding="14,8"
                                            Margin="20"
                                            VerticalAlignment="Top"
                                            HorizontalAlignment="Left">
                                        <TextBlock Text="{x:Bind ViewModel.TypeBadgeText, Mode=OneWay}"
                                                   FontSize="11"
                                                   FontWeight="SemiBold"
                                                   CharacterSpacing="80"
                                                   Foreground="White" />
                                    </Border>

                                    <!-- Bild Counter (links unten) -->
                                    <Border Background="#80000000"
                                            CornerRadius="{StaticResource RadiusMd}"
                                            Padding="12,8"
                                            Margin="20"
                                            VerticalAlignment="Bottom"
                                            HorizontalAlignment="Left">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <FontIcon Glyph="&#xE8B9;"
                                                      FontSize="14"
                                                      Foreground="White" />
                                            <TextBlock FontSize="14" FontWeight="Medium" Foreground="White">
                                                <Run Text="{x:Bind ViewModel.CurrentImageIndex, Mode=OneWay}" />
                                                <Run Text="/" />
                                                <Run Text="{x:Bind ViewModel.Property.ImageUrls.Count, Mode=OneWay}" />
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>

                                    <!-- Merken/Teilen Buttons (rechts oben) - Pill Design -->
                                    <StackPanel Orientation="Horizontal"
                                                Spacing="10"
                                                Margin="20"
                                                VerticalAlignment="Top"
                                                HorizontalAlignment="Right">
                                        <Button Background="#80000000"
                                                BorderThickness="0"
                                                Padding="16,10"
                                                CornerRadius="{StaticResource RadiusMd}"
                                                Visibility="{x:Bind ViewModel.IsAuthenticated, Mode=OneWay}"
                                                Command="{x:Bind ViewModel.ToggleFavoriteCommand}">
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <FontIcon Glyph="{x:Bind ViewModel.FavoriteButtonIcon, Mode=OneWay}"
                                                          FontSize="16"
                                                          Foreground="White" />
                                                <TextBlock Text="Merken"
                                                           FontSize="14"
                                                           FontWeight="Medium"
                                                           Foreground="White"
                                                           VerticalAlignment="Center" />
                                            </StackPanel>
                                        </Button>
                                        <Button Background="#80000000"
                                                BorderThickness="0"
                                                Padding="16,10"
                                                CornerRadius="{StaticResource RadiusMd}"
                                                Command="{x:Bind ViewModel.SharePropertyCommand}">
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <FontIcon Glyph="&#xE72D;"
                                                          FontSize="16"
                                                          Foreground="White" />
                                                <TextBlock Text="Teilen"
                                                           FontSize="14"
                                                           FontWeight="Medium"
                                                           Foreground="White"
                                                           VerticalAlignment="Center" />
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Content: volle Breite -->
                            <StackPanel Spacing="16" Margin="0,0,0,80">

                                <!-- Strukturierte Datentabelle -->
                                <ItemsRepeater ItemsSource="{x:Bind ViewModel.DetailSections, Mode=OneWay}"
                                               Margin="0,8,0,0">
                                    <ItemsRepeater.ItemTemplate>
                                        <DataTemplate x:DataType="models:PropertyDetailSection">
                                            <StackPanel Margin="0,0,0,20">
                                                <!-- Section Header -->
                                                <TextBlock Text="{x:Bind Title}"
                                                           FontSize="13"
                                                           FontWeight="SemiBold"
                                                           CharacterSpacing="60"
                                                           Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                                           Margin="0,0,0,10" />

                                                <!-- Section Items -->
                                                <Border Background="{ThemeResource SolidBackgroundFillColorSecondary}"
                                                        CornerRadius="{StaticResource RadiusMd}"
                                                        Padding="0">
                                                    <ItemsRepeater ItemsSource="{x:Bind Items}">
                                                        <ItemsRepeater.ItemTemplate>
                                                            <DataTemplate x:DataType="models:PropertyDetailItem">
                                                                <StackPanel>
                                                                    <Grid Padding="16,12"
                                                                          ColumnSpacing="16">
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="*" />
                                                                            <ColumnDefinition Width="Auto" />
                                                                        </Grid.ColumnDefinitions>
                                                                        <TextBlock Grid.Column="0"
                                                                                   Text="{x:Bind Label}"
                                                                                   FontSize="15"
                                                                                   FontWeight="SemiBold"
                                                                                   Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                                                                   VerticalAlignment="Center" />
                                                                        <TextBlock Grid.Column="1"
                                                                                   Text="{x:Bind Value}"
                                                                                   FontSize="15"
                                                                                   Foreground="{ThemeResource OnSurfaceBrush}"
                                                                                   VerticalAlignment="Center"
                                                                                   TextAlignment="Right" />
                                                                    </Grid>
                                                                    <!-- Trennlinie -->
                                                                    <Border Height="1"
                                                                            Background="{ThemeResource BorderBrush}"
                                                                            Opacity="0.3"
                                                                            Margin="16,0" />
                                                                </StackPanel>
                                                            </DataTemplate>
                                                        </ItemsRepeater.ItemTemplate>
                                                    </ItemsRepeater>
                                                </Border>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>

                                <!-- Beschreibung / Hinweise -->
                                <StackPanel Spacing="10"
                                            Margin="0,4,0,0"
                                            Visibility="{x:Bind ViewModel.HasDescription, Mode=OneWay}">
                                    <TextBlock Text="BESCHREIBUNG / HINWEISE"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               CharacterSpacing="60"
                                               Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                                    <TextBlock Text="{x:Bind ViewModel.Description, Mode=OneWay}"
                                               FontSize="15"
                                               Foreground="{ThemeResource OnSurfaceBrush}"
                                               TextWrapping="Wrap"
                                               LineHeight="24" />
                                </StackPanel>

                                <!-- Dokumente -->
                                <StackPanel Spacing="10"
                                            Margin="0,4,0,0"
                                            Visibility="{x:Bind ViewModel.HasDocuments, Mode=OneWay}">
                                    <TextBlock Text="DOKUMENTE"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               CharacterSpacing="60"
                                               Foreground="{ThemeResource OnSurfaceVariantBrush}" />

                                    <StackPanel Spacing="8">
                                        <Button HorizontalAlignment="Stretch"
                                                HorizontalContentAlignment="Left"
                                                Padding="16,14"
                                                Background="{ThemeResource SolidBackgroundFillColorSecondary}"
                                                CornerRadius="{StaticResource RadiusMd}"
                                                BorderThickness="0"
                                                CommandParameter="{x:Bind ViewModel.FloorPlanUrl, Mode=OneWay}"
                                                Command="{x:Bind ViewModel.OpenDocumentCommand}"
                                                Visibility="{x:Bind ViewModel.FloorPlanUrl, Converter={StaticResource NullToCollapsedConverter}, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="12">
                                                <FontIcon Glyph="&#xE8A5;" FontSize="18" Foreground="{ThemeResource AccentBrush}" />
                                                <TextBlock Text="Grundriss" FontSize="15" FontWeight="Medium" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </Button>

                                        <Button HorizontalAlignment="Stretch"
                                                HorizontalContentAlignment="Left"
                                                Padding="16,14"
                                                Background="{ThemeResource SolidBackgroundFillColorSecondary}"
                                                CornerRadius="{StaticResource RadiusMd}"
                                                BorderThickness="0"
                                                CommandParameter="{x:Bind ViewModel.SitePlanUrl, Mode=OneWay}"
                                                Command="{x:Bind ViewModel.OpenDocumentCommand}"
                                                Visibility="{x:Bind ViewModel.SitePlanUrl, Converter={StaticResource NullToCollapsedConverter}, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="12">
                                                <FontIcon Glyph="&#xE909;" FontSize="18" Foreground="{ThemeResource AccentBrush}" />
                                                <TextBlock Text="Lageplan" FontSize="15" FontWeight="Medium" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </Button>

                                        <Button HorizontalAlignment="Stretch"
                                                HorizontalContentAlignment="Left"
                                                Padding="16,14"
                                                Background="{ThemeResource SolidBackgroundFillColorSecondary}"
                                                CornerRadius="{StaticResource RadiusMd}"
                                                BorderThickness="0"
                                                CommandParameter="{x:Bind ViewModel.LongAppraisalUrl, Mode=OneWay}"
                                                Command="{x:Bind ViewModel.OpenDocumentCommand}"
                                                Visibility="{x:Bind ViewModel.LongAppraisalUrl, Converter={StaticResource NullToCollapsedConverter}, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="12">
                                                <FontIcon Glyph="&#xE9F9;" FontSize="18" Foreground="{ThemeResource AccentBrush}" />
                                                <TextBlock Text="Langschaetzung" FontSize="15" FontWeight="Medium" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </Button>

                                        <Button HorizontalAlignment="Stretch"
                                                HorizontalContentAlignment="Left"
                                                Padding="16,14"
                                                Background="{ThemeResource SolidBackgroundFillColorSecondary}"
                                                CornerRadius="{StaticResource RadiusMd}"
                                                BorderThickness="0"
                                                CommandParameter="{x:Bind ViewModel.ShortAppraisalUrl, Mode=OneWay}"
                                                Command="{x:Bind ViewModel.OpenDocumentCommand}"
                                                Visibility="{x:Bind ViewModel.ShortAppraisalUrl, Converter={StaticResource NullToCollapsedConverter}, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="12">
                                                <FontIcon Glyph="&#xE9F9;" FontSize="18" Foreground="{ThemeResource AccentBrush}" />
                                                <TextBlock Text="Kurzschaetzung" FontSize="15" FontWeight="Medium" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>

                <!-- Desktop Sticky Bottom-Sheet (Gericht + Edikt) -->
                <Border VerticalAlignment="Bottom"
                        Visibility="{utu:Responsive Narrowest=Collapsed, Normal=Visible}"
                        Background="{ThemeResource SurfaceBrush}"
                        BorderBrush="{ThemeResource BorderBrush}"
                        BorderThickness="0,0.5,0,0"
                        Translation="0,0,32">
                    <Border.Shadow>
                        <ThemeShadow />
                    </Border.Shadow>
                    <StackPanel MaxWidth="1200"
                                HorizontalAlignment="Center">

                        <!-- Header-Bar (immer sichtbar, klickbar) -->
                        <Button HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Stretch"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="40,16"
                                Command="{x:Bind ViewModel.ToggleContactExpandedCommand}">
                            <StackPanel Orientation="Horizontal"
                                        Spacing="16">
                                <!-- Gericht Name -->
                                <TextBlock Text="{x:Bind ViewModel.Property.SellerName, Mode=OneWay}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="{ThemeResource OnSurfaceBrush}"
                                           VerticalAlignment="Center" />

                                <!-- Trenner -->
                                <Border Width="1"
                                        Height="16"
                                        Background="{ThemeResource BorderBrush}"
                                        Opacity="0.5"
                                        VerticalAlignment="Center" />

                                <!-- Chevron + Label -->
                                <StackPanel Orientation="Horizontal"
                                            Spacing="6"
                                            VerticalAlignment="Center">
                                    <TextBlock Text="Details"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="{ThemeResource AccentBrush}"
                                               VerticalAlignment="Center" />
                                    <FontIcon Glyph="{x:Bind ViewModel.ContactExpandIcon, Mode=OneWay}"
                                              FontSize="12"
                                              Foreground="{ThemeResource AccentBrush}" />
                                </StackPanel>
                            </StackPanel>
                        </Button>

                        <!-- Aufklappbarer Bereich -->
                        <StackPanel Visibility="{x:Bind ViewModel.IsContactExpanded, Mode=OneWay}"
                                    Padding="40,0,40,20"
                                    Spacing="12">

                            <!-- Trennlinie -->
                            <Border Height="1"
                                    Background="{ThemeResource BorderBrush}"
                                    Opacity="0.3" />

                            <!-- Kopiert-Feedback -->
                            <TextBlock Text="{x:Bind ViewModel.CopyFeedback, Mode=OneWay}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{ThemeResource AccentBrush}"
                                       HorizontalAlignment="Center"
                                       Visibility="{x:Bind ViewModel.CopyFeedback, Converter={StaticResource NullToCollapsedConverter}, Mode=OneWay}" />

                            <!-- Edikt Button -->
                            <Button Background="{ThemeResource OnSurfaceBrush}"
                                    Foreground="{ThemeResource SurfaceBrush}"
                                    FontWeight="SemiBold"
                                    FontSize="14"
                                    Height="44"
                                    Padding="24,0"
                                    HorizontalAlignment="Left"
                                    CornerRadius="{StaticResource RadiusMd}"
                                    Command="{x:Bind ViewModel.OpenEdictCommand}"
                                    Visibility="{x:Bind ViewModel.EdictUrl, Converter={StaticResource NullToCollapsedConverter}, Mode=OneWay}">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE8A7;"
                                              FontSize="14"
                                              Foreground="{ThemeResource SurfaceBrush}" />
                                    <TextBlock Text="Edikt ansehen"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </uc:BusyOverlay>
    </Grid>
</pages:BasePage>
