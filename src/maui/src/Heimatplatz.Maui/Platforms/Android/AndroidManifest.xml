<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true">
		<!-- FCM-Notification-Payloads im Background zeigt Firebase selbst an (nicht Shiny):
		     ohne diese Defaults nimmt es das App-Icon -> grauer Klumpen in der Statusleiste -->
		<meta-data android:name="com.google.firebase.messaging.default_notification_icon" android:resource="@drawable/notification" />
		<!-- Eigener lowercase-Name: Xamarins Manifest-Generator lowercased Referenzen,
		     @color/colorAccent wuerde als "coloraccent" gesucht (APT2260) -->
		<meta-data android:name="com.google.firebase.messaging.default_notification_color" android:resource="@color/notification_accent" />
	</application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<!-- Android 13+ Runtime-Permission fuer Notifications (Shiny fragt zur Laufzeit an) -->
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
	<!-- Kamera fuer Foto-/Video-Aufnahme bei der KI-gestuetzten Inseratserstellung -->
	<uses-permission android:name="android.permission.CAMERA" />
	<!-- Mikrofon fuer Diktat (Shiny.Speech) und Video-Aufnahme -->
	<uses-permission android:name="android.permission.RECORD_AUDIO" />
	<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
	<!-- MediaPicker Capture auf aelteren Android-Versionen -->
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
	<!-- Sichtbarkeit der Kamera-Apps fuer MediaPicker Capture (Android 11+) -->
	<queries>
		<intent>
			<action android:name="android.media.action.IMAGE_CAPTURE" />
		</intent>
		<intent>
			<action android:name="android.media.action.VIDEO_CAPTURE" />
		</intent>
	</queries>
</manifest>