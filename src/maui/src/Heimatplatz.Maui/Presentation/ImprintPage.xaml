<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Heimatplatz.Maui.Presentation.ImprintPage"
             Title="Impressum">
    <Grid>
        <ActivityIndicator IsRunning="{Binding IsLoading}"
                           IsVisible="{Binding IsLoading}"
                           VerticalOptions="Center"
                           HorizontalOptions="Center" />

        <Label Text="{Binding ErrorMessage}"
               IsVisible="{Binding HasError}"
               TextColor="Red"
               Margin="16"
               VerticalOptions="Center"
               HorizontalOptions="Center" />

        <ScrollView IsVisible="{Binding IsLoaded}">
            <VerticalStackLayout Padding="16" Spacing="12">
                <Label Text="Impressum" FontSize="24" FontAttributes="Bold" />

                <Label Text="{Binding CompanyLine}" FontSize="16" FontAttributes="Bold" />
                <Label Text="{Binding OwnerLine}" />
                <Label Text="{Binding AddressLine}" />
                <Label Text="{Binding ContactLine}" />
                <Label Text="{Binding UidLine}" />

                <VerticalStackLayout Spacing="12"
                                     BindableLayout.ItemsSource="{Binding Sections}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="{Binding Title}" FontAttributes="Bold" FontSize="16" />
                                <Label Text="{Binding Content}" />
                            </VerticalStackLayout>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </VerticalStackLayout>

                <Label Text="{Binding VersionLine}" FontSize="12" TextColor="{StaticResource Gray500}" Margin="0,12,0,0" />
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
